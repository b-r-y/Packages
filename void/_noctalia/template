# this template for 'noctalia'
MAIN=$(curl -S "https://api.github.com/repos/noctalia-dev/noctalia-shell/git/refs/heads/v5")
SHA=$(echo $MAIN | jq -r '.object.sha[:7]')

COMMIT=$(curl -S "https://api.github.com/repos/noctalia-dev/noctalia-shell/commits/$SHA")
DATE=$(echo $COMMIT | jq -r '.commit.committer.date | split("T")[0] | gsub("-"; ".")')

pkgname=noctalia
version=5.0.0.r${DATE}.${SHA}
revision=0
build_style=meson
#configure_args="-Dbuildtype=release"
short_desc="Noctalia desktop shell"
maintainer="Noctalia <team@enoctalia.dev>"
license="MIT"
homepage="https://github.com/noctalia-dev/noctalia-shell"
 
hostmakedepends="meson ninja pkg-config git"
 
makedepends="
 wayland-devel
 wayland-protocols
 libepoxy-devel
 MesaLib-devel
 libglvnd-devel
 cairo-devel
 pango-devel
 fontconfig-devel
 freetype-devel
 harfbuzz-devel
 libxkbcommon-devel
 pipewire-devel
 libcurl-devel
 pam-devel
 libwebp-devel
 basu-devel
 libcurl-devel
 sdbus-c++-devel
 polkit-devel
 librsvg-devel
"
 
depends="
 wayland
 pipewire
 pam
 libwebp
 basu
"
 
CXXFLAGS+=" -std=c++20"

do_fetch() {
        git clone https://github.com/noctalia-dev/noctalia-shell.git --branch v5 ${wrksrc}
}
 
post_install() {
        if [ -f LICENSE ]; then
                vlicense LICENSE
        fi
 
        if [ ! -x "${DESTDIR}/usr/bin/noctalia" ]; then
                msg_error "Expected binary /usr/bin/noctalia not found.\n"
        fi
}